

# -----------------------------------------------------------------------------
# Environment variables for Intune-Deployment-App
# Copy or rename this file to ".env" and fill in real values before running the app.
# -----------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Supabase PostgreSQL connection string
# Obtain this from Supabase ➜ Settings ➜ Database ➜ Connection string.
# Replace <password> and <hash> with your project‑specific values.
# ---------------------------------------------------------------------------
DATABASE_URL=postgresql://postgres:<password>@db.<hash>.supabase.co:5432/postgres

# ---------------------------------------------------------------------------
# Microsoft Graph credentials (Azure AD app registration)
# ---------------------------------------------------------------------------
#GRAPH_CLIENT_ID=
#GRAPH_CLIENT_SECRET=
#GRAPH_TENANT_ID=

# ---------------------------------------------------------------------------
# Optional: base URL of your Supabase project (needed only if the API
# layer uses the Supabase client SDK directly).
# ---------------------------------------------------------------------------
# SUPABASE_URL=https://<your-project>.supabase.co
# SUPABASE_SERVICE_KEY=<service-role-key>

# ---------------------------------------------------------------------------
# FastAPI / Uvicorn settings (override defaults if desired)
# ---------------------------------------------------------------------------
# API_HOST=0.0.0.0
# API_PORT=8000